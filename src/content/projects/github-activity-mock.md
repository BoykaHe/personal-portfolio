# GitHub Activity Mock

This markdown file exists to reserve the content directory for future README rendering.

## Why it exists

- The UI can point at a local markdown source later
- The project model already knows where the content lives
- Phase 2A does not need the markdown renderer yet
